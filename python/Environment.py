from UtilityTTT import *


class Environment:

    def __init__( self, opponent ):
        self.opponent = opponent
        self.x = 0

    def env_start( self ):
        #return an observation based on the starting state
        # self.current_state = '.........'
        # return self.observation(self.current_state)
        
    def env_step( self, action ):

        opponent.


        
        #current state <= current state affected by action
        #test is new current_state is terminal
        #return ( reward, self.observation(self.current_state), is_terminal )

    def observation( self, state ):
        pass
